# 센서 참고 코드: 자체 git 저장소(Raph-Alpaca/sensor-lab)를 품고 있어 이 저장소에는 올리지 않음
reference/

# Firebase 에뮬레이터가 남기는 로그
firestore-debug.log
storage-debug.log
firebase-debug.log
.firebase/

# Netlify 빌드 때 deploy/netlify-redirects.mjs 가 만드는 파일
_redirects
